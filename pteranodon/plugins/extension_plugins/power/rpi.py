import subprocess


class RPi:
    """
    A class for acquring the power usage of a Raspberry pi 4
    """

    self __init__(self):
        try:
            raise subprocess.SubprocessError
        except subprocess.SubprocessError:
            raise RuntimeError("RPi could not be started")
